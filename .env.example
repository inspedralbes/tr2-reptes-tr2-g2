# ==============================================================================
# 🌍 GLOBAL ENV EXAMPLE
# Este archivo contiene TODAS las variables necesarias para el proyecto.
# Copia cada sección en su archivo correspondiente según se indica.
# ==============================================================================


# ==============================================================================
# 1. INFRAESTRUCTURA DOCKER
# 📂 Copiar en: ./.env  (En la raíz)
# ==============================================================================

# --- Base de Datos (PostgreSQL) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_DB=enginy_db
DB_PORT=5432

# --- Puertos de Servicios ---
API_PORT=4000
WEB_PORT=3000
ADMINER_PORT=8080

# --- General ---
NODE_ENV=development


# ==============================================================================
# 2. BACKEND API
# 📂 Copiar en: ./apps/api/.env
# ==============================================================================

# Debe coincidir con API_PORT
PORT=4000

# --- Conexión Prisma (Docker) ---
# IMPORTANTE: El host es 'db' (nombre del servicio en docker-compose)
DATABASE_URL="postgresql://postgres:root@db:5432/enginy_db?schema=public"

# --- Seguridad ---
JWT_SECRET="cambiar_esto_por_secreto_seguro_en_prod"

# --- CORS ---
# Dominios permitidos (Web Local + IP local para móvil)
CORS_ORIGIN="http://localhost:3000,http://localhost:8081"


# ==============================================================================
# 3. FRONTEND WEB (Next.js)
# 📂 Copiar en: ./apps/web/.env
# ==============================================================================

# URL de la API vista desde el Navegador (Chrome/Firefox)
# Aquí usamos 'localhost' porque el navegador está fuera de Docker
NEXT_PUBLIC_API_URL="http://localhost:4000/api"


# ==============================================================================
# 4. FRONTEND MOBILE (Expo/React Native)
# 📂 Copiar en: ./apps/mobile/.env
# ==============================================================================

# URL de la API vista desde el Móvil/Emulador
# ⚠️ IMPORTANTE: No uses 'localhost'. Usa tu IP LOCAL (ej: 192.168.1.39)
# Si usas Emulador Android Studio: 10.0.2.2
EXPO_PUBLIC_API_URL="http://TU_IP_LOCAL:4000/api"